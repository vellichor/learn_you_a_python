#!/usr/bin/env python3

rad_types = ['alpha', 'beta', 'gamma']

for rad_type in rad_types:
  print("Oh no! " + rad_type + " radiation!")

for x in range(6):
  print(x)

for x in range(len(rad_types)):
  print(str(x) + " kinds of radiation! Oh boy!")

